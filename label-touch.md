# 라벨전용 운영 화면 — 터치 2안

2026-09-15 `label-print.html`에 선택한 2안을 적용했습니다. `label-touch.js`가 기존 운영 화면 요소와 실제 인쇄·출력이력·생산완료 함수를 연결합니다. 시안 데이터를 운영으로 복사하지 않습니다. `label-touch.css`는 운영 화면에만 적용되며 실제 라벨 문서 스타일은 변경하지 않습니다.

## 출력과 생산 집계

- **내포장라벨출력:** 현재 작업의 라벨을 바로 인쇄합니다. `labelPrintLogs` 저장 요청을 보내지 않으므로 생산이력, 생산일보 전송량, 재고에 포함되지 않습니다. 내포장 인쇄 횟수도 서버에 보관하지 않습니다. 생산완료 후에도 내포장을 출력할 수 있습니다.
- **외포장라벨출력:** 기존대로 출력이력을 먼저 저장한 후 인쇄합니다. 외포장 실적만 생산완료 시 생산일보·재고에 반영합니다.
- 기존 저장된 출력이력은 전부 외포장으로 취급합니다. 내포장/외포장 구분을 위해 기존 출력 데이터와 생산완료 내역을 변경하지 않습니다. 전송 취소 기능은 별도 서버 RPC(아래 41번)를 사용합니다.
- 고정중량·매수와 중량 모드는 내포장/외포장 및 작업별로 분리됩니다. 화면을 새로고침하면 입력 설정은 작업지시의 기본값으로 돌아갑니다. 실제 출력이력은 서버에서 다시 읽습니다.
- 계근 모드는 현재 안정 중량으로 한 번에 1장 출력합니다. 인쇄 버튼을 눌렀을 때 중량을 고정해서 해당 라벨 기록에 사용하므로 저장 중 저울값이 변해도 인쇄 중량은 바뀌지 않습니다.

## 작업지시 변경

왼쪽에는 현재 작업 요약과 큰 **작업지시 변경** 버튼만 표시하고 계산기를 넓혔습니다. 버튼을 누르면 날짜·품목·LOT 검색이 가능한 작업지시 목록 팝업이 열립니다. 작업 행을 눌러야 전환되며 검색, 날짜 필터, 닫기, Escape만으로는 현재 작업과 중량 설정이 바뀌지 않습니다. 새로고침 시에도 해당 작업이 서버에 남아 있으면 선택을 유지합니다.

## 전송 취소와 재전송

전송 완료된 작업은 하단의 **전송 취소 · 수정하기**를 누릅니다. 확인창에서 작업명·생산일보 일자/번호와 취소 범위를 확인한 다음 **생산일보 삭제 · 전송 취소**를 누르면 다음을 한 트랜잭션으로 처리합니다.

- 현재 작업과 정확히 연결된 생산일보만 삭제 상태로 변경합니다. 원료 사용·생산입고·부자재 사용도 함께 취소하여 재고 계산에서 제외합니다.
- 사무실에서 해당 생산일보에 추가·수정한 내역도 취소 대상입니다. 다음 재전송은 라벨 출력이력으로 새 생산일보를 만들기 때문에 사무실의 수정내용이나 부자재 사용을 자동 복사하지 않습니다.
- 외포장 출력이력, 당시 라벨 스냅샷, 기존 생산일보 원문과 취소 감사기록은 보존합니다. 잘못 출력한 외포장 이력을 삭제하고 추가 출력한 뒤 **생산일보 재전송**할 수 있습니다.
- 기존 생산품 재고행에 출고·재투입·재고이동·재고조정 또는 다른 작업지시가 연결되어 있으면 취소를 거절합니다. 연결 내역은 사무실에서 먼저 확인해야 합니다. 기존 재고행 보호 검증을 그대로 호출합니다.
- 기존 라벨 PIN 세션을 사용하며 PIN을 다시 입력받지 않습니다. 일반 ERP 생산일보를 임의로 지우는 권한은 부여하지 않습니다.
- 실패·통신 끊김·응답 누락이면 먼저 잠금을 유지합니다. 새로고침하거나 동일 취소를 재시도하여 서버 상태를 확인합니다. 이미 취소된 같은 생산일보는 중복 취소되지 않으며, 재전송 이후 오래된 취소 요청은 새 생산일보에 적용되지 않습니다.
- 다른 사무실 화면에서 열어둔 부자재 입력을 저장해 취소된 생산일보의 사용내역을 되살리는 것도 차단합니다. 아직 DB로 이관되지 않은 기존 생산일보의 부자재 입력은 유지합니다.

서버: `supabase/schema-rpc-41-label-production-cancel.sql` / `supabase/migrations/20260915090000_label_production_cancel.sql`. 39·40 설치 후 추가 설치하며 설치 자체는 업무 데이터를 수정하지 않습니다. `tools/check-label-cancel-db.ps1`는 항상 되돌리는 가상 데이터 검증이고, `-Apply`는 검증 성공 시 함수 정의만 설치합니다. 실제 생산일보는 테스트로 취소하지 않습니다.

## 생산품목/스펙 변경 제안 — 미적용

작업지시의 원료·LOT는 유지하고 별도의 **생산품목/스펙 선택** 팝업에서 등록된 생산품을 고르는 방식을 권장합니다. 예: 같은 돈등심 원료에서 돈까스용 10mm, 잡채용 3mm 등. 현재 선택한 생산품을 크게 표시하고 변경은 다음 출력부터 적용합니다.

이미 출력한 라벨은 당시 품목·스펙으로 재출력하며, 외포장 생산량은 품목별로 따로 집계합니다. 생산완료 시에는 하나의 생산일보에 원료 투입은 한 번만, 생산품은 여러 행으로 전송합니다. 생산품별 품목보고번호·소비기한·규격은 품목 기준정보를 따릅니다. 단순 이름 변경이 아닌 재고 품목 연결이 필요하므로 선택 UI뿐 아니라 작업지시의 허용 생산품 구조와 서버 출력 검증도 함께 바꿔야 합니다. 이 제안은 사용자의 선택 전까지 운영에 적용하지 않습니다.

## 재출력과 미리보기

하단 **재출력**은 현재 작업의 외포장 이력을 4건씩 표시합니다. 여러 페이지에서 선택한 라벨을 함께 재출력할 수 있습니다. 당시 작업·LOT·중량·라벨 크기를 유지하며 생산량이나 출력이력 건수는 추가되지 않습니다. 삭제된 라벨은 재출력 선택할 수 없습니다. 개별 삭제는 기존 사유 입력과 생산완료 잠금 규칙을 유지합니다.

미리보기는 요약이 아니라 `buildLabelBody`와 `DBMTLabelPrint.createDocument`로 만든 **실제 라벨 전체 문서**를 화면 안 팝업에 보여줍니다. 미리보기는 스크립트 실행이 금지된 iframe이며 인쇄나 저장을 실행하지 않습니다. 출력 버튼은 기존 숨김 iframe 원클릭 인쇄를 그대로 사용합니다.

인쇄 실패 전 이력이 저장된 경우 기록을 지우거나 새 외포장 출력으로 재시도하지 말고 실제 출력 여부를 먼저 확인한 뒤 기존 이력을 재출력합니다. 브라우저는 프린터에서 실제 종이가 나왔는지까지 확인할 수 없습니다.

## 현장 저울 연결

현장 확인값: A&D FG-150KAL, **COM2 / 2400 bps / 7 data bits / even parity / 1 stop bit**. `ST,+00000.24 kg` 형식의 수신값을 사용합니다.

1. SM에서 저울 포트를 사용하지 않도록 SM을 정상 종료합니다. 다른 프로그램을 강제 종료하거나 포트를 자동 탐색하지 않습니다.
2. 라벨전용 Edge 화면에서 **저울 연결 → 연결**을 누르고 COM2를 선택합니다.
3. 현재 계근중량과 저울 표시가 일치하는지 확인합니다. 용기무게는 저울에서 설정합니다. 프로그램은 수신 중량을 그대로 사용하며 별도 용기 공제를 하지 않습니다.
4. 내포장 또는 외포장에서 **계근중량**을 선택합니다. 안정된 양수 중량일 때 출력할 수 있습니다.
5. SM을 다시 쓰려면 **연결 해제**를 누른 뒤 SM을 실행합니다. 브라우저를 다시 열면 연결 버튼으로 재연결합니다.

저울에 명령을 보내지 않는 읽기 전용 연결입니다. 연결 전·연결 끊김·흔들림·0/음수·형식 오류·3초 넘는 수신 중단 시 계근 출력은 차단하고 이전 값을 지웁니다. 고정중량 출력은 그대로 사용할 수 있습니다. 장치 선택은 사용자 클릭으로만 수행하며 사무실 PC의 포트나 프린터 설정을 변경하지 않습니다.

브라우저 연결 구현 참고: [Chrome Web Serial 공식 문서](https://developer.chrome.com/docs/capabilities/serial).

## 배포 및 검증

- ERP의 라벨전용 링크 버전을 갱신했습니다. 기존 바로가기는 그대로 사용할 수 있으며 새 화면이 안 보이면 `Ctrl+F5`로 새로고침합니다.
- 1280×1024 터치 화면을 기준으로 오른쪽 위 계근중량, 왼쪽 위 현재 작업/목록 팝업 버튼, 왼쪽 아래 큰 계산기, 오른쪽 상하 내포장·외포장 버튼을 배치했습니다. 전체화면 버튼 또는 F11을 사용할 수 있습니다.
- `tools/test-label-touch-live.cjs`: 실제 페이지 DOM에서 모의 RPC·시리얼·프린터를 사용해 작업 팝업/검색 중 선택 유지, 취소 확인/실패/모호한 응답/재시도/재시작/재전송, 내포장 무저장, 외포장 집계, 선택 재출력, 실제 미리보기, 계산기, 중량 설정, 계근 신호·수신 중단·해제, 반응형을 검사합니다.
- `tools/test-label-hidden-print.cjs`, `tools/test-workorder-label.cjs`, `tools/test-label-production.cjs`: 기존 인쇄·입력·생산완료·삭제 후 재전송 회귀 검증입니다.
- `tools/test-label-print-layout.cjs`: 기존 70×100/100×100 mm 인쇄 문서 크기, 페이지 수, 우측 여백, HACCP 마크, 하단 문구를 검사합니다.

자동 테스트는 실제 생산 데이터를 쓰거나 실제 프린터/저울을 조작하지 않습니다. POS-PC에서의 실물 계근 일치와 인쇄는 현장 확인이 필요합니다.
